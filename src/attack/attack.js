export default class Attack {

    constructor(name, element, energy, damage) {
        this.name = name
        this.element = element
        this.energy = energy
        this.damage = damage
    }
}
